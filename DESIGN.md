# 商談 次回アクション日リマインド Routine 設計

保有商談の「次回アクション日」を毎営業日クローリングし、**期限超過**および**当日**の案件を
Slackの商談チャンネルにメンション付きでリマインドする仕組みの設計。

- 対象ユーザー：荒舩 健太（Salesforce OwnerId `005GC00000kynm7YAA` / Slack `U033MA255R6`）
- 実行基盤：Claude Code Routine（サーバ側スケジューラ、毎営業日朝）
- 実装形態：スキル（`SKILL.md`）＋ Routine 登録。スキルが実行内容、Routine がスケジュールを持つ

---

## 1. 全体像

```
[Routine 07:57 JST 平日]
        │
        ▼
[新規セッション起動] ── connectors: FFDE, Slack
        │
        ▼
STEP1  ClickHouse（FFDE）で保有商談を1クエリ取得
        │        └─ 期限超過 / 当日 / 未設定 に仕分け
        ▼
STEP2  対象0件なら終了（Slack投稿なし・完了通知のみ）
        │
        ▼
STEP3  商談 → Slackチャンネル解決（キャッシュ優先／未解決のみ検索）
        │
        ▼
STEP4  重複投稿チェック（当日同一商談への投稿済みログを参照）
        │
        ▼
STEP5  各商談チャンネルへメンション付き投稿
        │
        ▼
STEP6  状態シート更新（チャンネルキャッシュ／投稿ログ）
        │
        ▼
STEP7  自分宛サマリー（Routine完了プッシュ通知＋DM）
```

---

## 2. 実データで確認済みの前提

設計の土台になる4点は、想像ではなく実データで確認した。

| # | 確認事項 | 結果 |
|---|---|---|
| 1 | 次回アクション日のフィールド | `salesforce_opportunity.next_action_date__c`（`DateTime64(3,'Asia/Tokyo')`）／内容は `next_action_content__c` |
| 2 | 対象件数の規模 | 荒舩Owner・オープン商談 **54件**。うち期限超過 **4件**、当日 **1件**、未来 49件、日付未設定 **0件**（2026-07-29時点） |
| 3 | 商談チャンネルの実在と命名 | Salesforce連携（Slack Sales Elevate）が**商談ごとに自動でチャンネルを作成**。チャンネル名＝商談名。検証した4商談すべてに対応チャンネルが存在 |
| 4 | メンション先の解決手段 | `salesforce_user.SlackID__c` に Slack User ID が格納済み（荒舩 `U033MA255R6` / 檜垣 `U08DGSES1J9` / 木村 `U083B7V29K7`）。Slack側検索に頼らずSFデータで解決できる |

確認できたチャンネル例：

| 商談 | チャンネルID |
|---|---|
| UC_One_株式会社アドバンテッジリスクマネジメント_商談記録の高度化… | `C0BG873MPR9` |
| UC_ENT_株式会社エイチ・アイ・エス_継続接点_AI Autofillでの… | `C0BFVJ3H62W` |
| UC_One_富士フイルムメディカル株式会社_Sales Portal実行支援_継続接点 | `C0AN9G48KK5` |
| UC_One_日本3Dプリンター株式会社_MCP | `C0BJME28Y4W` |

> 補足：`slack_search_channels` の結果はチャンネル名が `ZC:<channel_id>:<商談名>` という形で返る。
> 実チャンネルIDは permalink から取得する。

---

## 3. STEP1：データ取得設計

`clickhouse-salesforce` スキル経由で `FFDE:query_clickhouse` を使う（Salesforce MCPは使わない）。
**1クエリで3バケットまとめて取得**し、アプリ側で仕分ける。

```sql
SELECT
  Id,
  Name,
  StageName,
  toDate(next_action_date__c)                          AS na_date,
  ifNull(next_action_content__c, '')                   AS na_content,
  ifNull(ACV__c, 0)                                    AS acv,
  OwnerId,
  ifNull(subhandler__c, '')                            AS subhandler,
  dateDiff('day', toDate(next_action_date__c), toDate(now('Asia/Tokyo'))) AS days_overdue
FROM salesforce_opportunity
WHERE IsClosed = false
  AND OwnerId = '005GC00000kynm7YAA'
  AND (next_action_date__c IS NULL
       OR next_action_date__c < toDate(now('Asia/Tokyo')) + 1)
ORDER BY na_date
LIMIT 200
```

### 仕分けルール

| バケット | 条件 | 投稿トーン |
|---|---|---|
| 🔴 期限超過 | `days_overdue >= 1` | 超過日数を明示。8日以上は「⚠️要棚卸し」を付す |
| 🟡 本日 | `days_overdue = 0` | 当日アクションの確認 |
| ⚪️ 未設定 | `next_action_date__c IS NULL` | 日付入力を促す（現状0件だが将来の抜け漏れ検知として残す） |

### 日付境界の重要な注意（設計上の最大の罠）

ClickHouseサーバのタイムゾーンは **UTC**。実測で `now()` は UTC を返す（JST 19:06 の時点で `10:06`）。
そのため **`today()` を使うと 00:00〜09:00 JST の実行で「前日」判定になり、当日案件を取り逃す**。

- ❌ `next_action_date__c = today()`
- ✅ `next_action_date__c >= toDate(now('Asia/Tokyo')) AND next_action_date__c < toDate(now('Asia/Tokyo')) + 1`

朝7:57 実行はまさにこの危険時間帯なので、`toDate(now('Asia/Tokyo'))` を**必ず**使う。
実測で両式の一致（期限超過4件／当日1件）を確認済み。

### その他のクエリ規約（clickhouse-salesforce スキル準拠）

- `IsDeleted` は存在しない。WHERE句に書かない
- `ACV__c` はキャスト不要（`toFloat64OrNull()` を付けるとCode 43エラー）
- 日付上限は `<= 月末日` ではなく `< 翌月1日` 形式
- 一覧系は LIMIT 必須

### 同期ラグ

`salesforce_opportunity` の最新 `LastModifiedDate` は実行時刻の約1時間前（実測：JST 19:06 時点で 17:56）。
ニアリアルタイム同期のため朝の実行には十分だが、**「当日朝にSalesforceで直した内容は反映されていない可能性がある」**
点をメッセージ末尾に注記する。

---

## 4. STEP3：商談 → Slackチャンネル解決設計

商談とチャンネルを結ぶIDは**Salesforce側に存在しない**（同期済み104カラムにSlack関連フィールドなし）。
名前一致で解決するしかないため、**キャッシュ前提**の設計にする。

```
商談ID
  │
  ├─ キャッシュ（状態シート）にヒット → channel_id をそのまま使う   ← 通常はここで終わる
  │
  └─ ミス → slack_search_channels(商談名の特徴語) で検索
              ├─ 1件ヒット → channel_id をキャッシュに登録
              ├─ 0件       → status=notfound で記録し、フォールバック投稿へ
              └─ 複数ヒット → 商談名完全一致を優先。決まらなければ notfound 扱い
```

**なぜキャッシュが必須か**：名前検索は1商談1コールかかり、かつ曖昧マッチのリスクがある。
毎日54件を検索し直すのは無駄で、誤爆（別商談のチャンネルへ投稿）が最悪の事故になる。
リマインド対象は日々5件前後なので、キャッシュが効けば検索はほぼ0回になる。

**検索クエリの作り方**：商談名は `UC_One_株式会社◯◯_◯◯` 形式。
`_` 区切りで企業名とテーマを抽出し、`企業名 テーマ` の形で検索する（実測でこの形が4/4ヒット）。

### 外部共有チャンネルのガード（重要）

Slack MCP は **Slack Connect（外部共有）チャンネルへ投稿できない**。
加えて、社内向けリマインドが顧客に見えるのは事故なので、投稿前に必ず確認する。

- 商談チャンネルは Salesforce 連携が `ninout.slack.com` に作る社内公開チャンネル（`public_channel`）→ 原則OK
- `#ext-` 始まり／`private_channel` で外部共有のものは **投稿せずスキップ**し、status=`ext_skip` で記録
- 判定は初回解決時に行い、結果をキャッシュに持たせる（毎日判定しない）

---

## 5. STEP5：メンション設計と「通知が飛ばない」問題

### メンション対象

`salesforce_user.SlackID__c` から解決する。

| 役割 | 解決元 | メンションするか |
|---|---|---|
| 商談Owner（荒舩） | `OwnerId` → `SlackID__c` | する |
| サブ担当 | `subhandler__c` → `SlackID__c` | する（値がある場合のみ） |
| それ以外の参加者 | — | しない（チャンネル参加者への無用な通知を避ける） |

`SlackID__c` が空のユーザーは名前表示のみにフォールバックし、メンションを省く（`<@>` 不正形式の投稿を防ぐ）。

### ⚠️ 設計上の落とし穴：自分宛メンションは通知されない

Slack MCP は**荒舩さん本人のトークンで投稿する**（投稿者＝荒舩さん）。
Slackは**自分の投稿では自分に通知を出さない**ため、
「自分のチャンネル投稿で自分をメンションする」構成だと**リマインドとして鳴らない**可能性が高い。

これは仕組みの目的（自分が気づく）を損なうので、二重化する：

| 経路 | 役割 | 通知の確実性 |
|---|---|---|
| 商談チャンネル投稿（メンション付き） | **記録と関係者への可視化**。サブ担当には通知が飛ぶ | 自分には飛ばない想定 |
| Routine完了プッシュ通知（`notifications: {push: true}`） | **自分への実質的なリマインド**（スマホに届く） | 確実 |
| 自分へのDM（`slack_send_message` の channel_id に `U033MA255R6`） | Slack内で一覧を見返す用 | 自己DMも同様に通知が弱い可能性あり |

**初回セットアップ時に、テスト投稿1本で「自分宛メンションが通知されるか」を実測して確定させる**。
飛ばないことが確認できたら、自分への到達はプッシュ通知を主経路とする。

---

## 6. メッセージ設計

### 商談チャンネルへの投稿（1商談1メッセージ）

```
<@U033MA255R6> 🔴 次回アクション日が 3日 超過しています

*商談*：UC_One_株式会社アドバンテッジリスクマネジメント_商談記録の高度化による営業イネーブルメント支援
*フェーズ*：P2 課題の特定 ／ *ACV*：¥1,200,000
*次回アクション日*：2026-07-21（3日超過）
*次回アクション内容*：ゴール：商談記録の主要な分析担当とのコネクション獲得

次回アクション日の更新、または実施済みなら日付の巻き直しをお願いします。
<商談URL|Salesforceで開く>
```

当日案件は `🟡 本日が次回アクション日です` に差し替え、超過日数の行を省く。

- 商談URL：`https://creativesurvey.lightning.force.com/lightning/r/Opportunity/{Id}/view`
- `next_action_content__c` は改行を含むため **200文字で切り、改行はスペースに正規化**する
- 全角スペースは Slack投稿でエラー要因になりうるため半角スペース2つに置換（スキル既定ルール）
- 1メッセージ5,000文字上限。1商談1メッセージなら超えない

### 自分宛サマリー

```
📋 次回アクション リマインド（2026-07-29）

🔴 期限超過 4件
 ・[8日超過] UC_One_◯◯_◯◯（P2 / ¥1,200,000）
 ・[5日超過] ...
🟡 本日 1件
 ・UC_One_日本3Dプリンター株式会社_MCP（P3 / ¥360,000）
⚪️ 日付未設定 0件

投稿：4チャンネル成功 / 1件チャンネル未特定（下記）
未特定：UC_One_エスリード株式会社_評価査定表_Shot
※ Salesforce同期は約1時間前時点のデータです
```

---

## 7. STEP4/6：状態管理（重複投稿防止・キャッシュ）

Routine は毎日新規セッションで動くため、セッション内メモリに状態を持てない。
**FFDE のヘッドレスシート**に2枚持たせる（コンテナが破棄されても残り、人間も中身を見られる）。

### シートA：チャンネルキャッシュ

| opportunity_id | opportunity_name | channel_id | channel_name | status | resolved_at |
|---|---|---|---|---|---|
| 006Q900001mcKUcIAM | UC_One_株式会社アドバンテッジ… | C0BG873MPR9 | ZC:…_商談記録の高度化… | ok | 2026-07-29 |

`status` は `ok` / `notfound` / `ext_skip`。`notfound` は7日おきに再解決を試みる
（チャンネルが後から作られるケースがあるため。実測でも商談作成の数日後にチャンネルが立っている例あり）。

### シートB：投稿ログ

| run_date | opportunity_id | bucket | channel_id | message_ts | status |
|---|---|---|---|---|---|

**投稿前に必ず「同一 run_date × opportunity_id」の `ok` レコードがないか確認する。**
Routine の再実行や手動実行で、顧客も見る可能性のあるチャンネルに同じ督促を二重投稿するのが
この仕組みで一番避けたい事故。

---

## 8. Routine 設定

`mcp__Claude_Code_Remote__create_trigger` で登録する。

| 項目 | 値 | 理由 |
|---|---|---|
| cron | `45 23 * * 0-4` | **UTC指定**。23:45 UTC = 翌08:45 JST。日〜木 UTC = 月〜金 JST（平日のみ）<br>※当初 `57 22`（07:57 JST）で作成したが、運用側で08:45 JSTに変更 |
| create_new_session_on_fire | `true` | 毎回クリーンな状態で実行。会話履歴に依存しない |
| connectors | `["FFDE", "Slack"]` | データ取得と投稿に必要な最小構成 |
| notifications | `{push: true}` | **自分への実質的なリマインド経路**（§5参照） |
| prompt | スキルを名前で呼ぶ自己完結型の指示文 | 新規セッションは文脈ゼロのため |

- 分を `:00` ではなく `:57` にしているのは、負荷が毎時0分に集中するのを避けるため
- **`CronCreate` は使わない**：セッション限定・7日で失効するため、恒久ルーティンに使えない
- 時刻は「朝の始業前に見られる」ことを優先して 07:57 JST。同期ラグ約1時間を考えると
  実質「前日夜〜当日早朝のSalesforce更新は反映済み」になる

### 登録済みRoutine（2026-07-29）

| 項目 | 実際の値 |
|---|---|
| trigger_id | `trig_01Fkj6ipZ111MJTeBoZPqCAr` |
| cron | `45 23 * * 0-4`（UTC）＝ 平日 08:45 JST |
| コネクタ | FFDE / Slack（付与済み・確認済み） |
| 通知 | プッシュ通知 ON |
| 状態 | **有効** |

### コネクタ付与についての注意（ハマりどころ）

`create_trigger` ツールの `connectors` パラメータは**この組織では利用できない**
（`connectors parameter is not available for this organization`）。ツール経由で作成した
Routineはコネクタ無しで登録され、発火したセッションは `mcp__*` ツールを一切持たないため
**FFDEもSlackも使えず機能しない**。

対処：**claude.ai の Routines UI から当該Routineに FFDE と Slack を付与する**。
付与後は `update_trigger` / `list_triggers` のレスポンスに `mcp_connections` が現れるので、
そこで実際に紐づいたか確認できる。今後同様のRoutineを作る際も同じ手順が必要。

### スキルの置き場所（2026-07-30 変更）

**現行：Claude本体にインストール済み**（`pipeline-coverage-daily` と同じユーザーレベル）。
Routine のプロンプトは `deal-next-action-reminder` スキルを名前で呼ぶだけの形に簡略化した。

リポジトリ側の `.claude/skills/deal-next-action-reminder/SKILL.md` は設計と実装を
同じ場所に残すための控えとして維持する。**両者を変更したときは必ず同期すること**
（実行に使われるのはインストール済みの方）。

当初はリポジトリ配置のみとし、Routineのプロンプトで作業ブランチを `git checkout` させていたが、
この方式は初回の定期実行で失敗した（§13参照）。

---

## 9. 異常系の扱い

| 事象 | 挙動 |
|---|---|
| ClickHouseがエラー | スキル既定のリトライ戦略に従う（1回目は即リトライ／2回連続でクエリ単純化／4回でユーザー報告）。取得失敗時は**Slack投稿を一切行わない**（部分投稿を作らない） |
| 対象0件 | Slack投稿なし。完了通知のみ「本日対象なし」 |
| チャンネル未特定 | その商談はスキップし、自分宛サマリーに「未特定」として列挙。**推測で別チャンネルに投稿しない** |
| 外部共有チャンネル | 投稿せずスキップ。`ext_skip` として記録し、サマリーに理由付きで列挙 |
| 一部チャンネルの投稿失敗 | 残りは投稿を続行。失敗分をサマリーに列挙（1件の失敗で全体を止めない） |
| 同日重複実行 | 投稿ログで検出し、投稿をスキップして「既に投稿済み」と報告 |

---

## 10. 設定パラメータ

スキル冒頭に定数としてまとめ、変更時の修正箇所を1箇所に閉じる。

| パラメータ | 既定値 |
|---|---|
| 対象OwnerId | `005GC00000kynm7YAA`（荒舩） |
| 対象範囲 | `IsClosed = false`（全フェーズ） |
| 期限超過の警告閾値 | 8日以上で「⚠️要棚卸し」 |
| 実行スケジュール | 平日 07:57 JST |
| アクション内容の最大文字数 | 200文字 |
| 1回の最大投稿件数 | 15件（超過分はサマリーのみ。暴走時の安全弁） |

---

## 11. 初回テスト実行の結果（2026-07-29）

対象5件すべて投稿成功。スキップ・失敗なし。

| 商談 | バケット | チャンネル | 結果 |
|---|---|---|---|
| UC_One_株式会社アドバンテッジリスクマネジメント_商談記録の高度化… | 8日超過 ⚠️要棚卸し | `C0BG873MPR9` | ok |
| UC_ENT_株式会社エイチ・アイ・エス_継続接点_AI Autofillでの… | 5日超過 | `C0BFVJ3H62W` | ok |
| UC_One_エスリード株式会社_評価査定表_Shot | 5日超過 | `C0BJ9DKVBNC` | ok |
| UC_One_富士フイルムメディカル株式会社_Sales Portal実行支援_継続接点 | 2日超過 | `C0AN9G48KK5` | ok |
| UC_One_日本3Dプリンター株式会社_MCP | 本日 | `C0BJME28Y4W` | ok |

検証できたこと：

- **書式**：`**bold**` はSlackの太字に、絵文字・リンクラベル・`<@ID>` メンションすべて正しく変換された
- **メンション解決**：`SlackID__c` 由来のIDが実ユーザー（荒舩・木村・檜垣）に解決された
- **重複排除**：日本3Dプリンター案件はOwnerとサブ担当が同一人物のため、メンションを1つに集約した
- **投稿者**：荒舩さん本人として投稿され、末尾に「Claudeを使用して送信されました」が付く
- **チャンネル解決**：5件すべて名前検索1回で特定でき、キャッシュに登録済み（次回以降は検索0回）

未検証：**自分宛メンションの通知が鳴るか**（§5の懸念）。実機で確認が必要。

## 12. 未決事項

1. **対象範囲**：Ownerのみか、サブ担当（`subhandler__c`）の案件も含めるか。
   `pipeline-coverage-daily` は `subhandler__c` ベースで集計している一方、FY27のOKRは
   OwnerId単独判定。本設計の既定は **Ownerのみ**
2. **メンション対象**：自分だけか、サブ担当も一緒にメンションするか（既定：サブ担当も含む）
3. **未来案件の扱い**：「翌営業日が期限」の予告リマインドを入れるか（既定：入れない）
4. **投稿頻度**：期限超過案件を毎日督促し続けるか、超過後は3日おきに落とすか（既定：毎日）

---

## 13. 運用ログ

### 2026-07-29 初回テスト実行（手動）
対象5件すべて投稿成功。詳細は §11。

### 2026-07-30 08:45 JST 初回定期実行 — ❌ 何も投稿されず

`last_fired_at` は記録されているのに、投稿ログに `2026-07-30` の行が1件もなく、
商談チャンネルの最新メッセージも前日19:25のままだった（＝1件も投稿されていない）。

このときのRoutineプロンプトは、スキルがデフォルトブランチに無いため
`git fetch` → `git checkout` で作業ブランチを取得させる方式だった。**Routineの新規セッションで
この回り道が成立しないと、以降の全STEPに到達できず沈黙して終わる**構造だったのが原因と考えられる。

対処：
1. スキルをClaude本体にインストール（ユーザーレベル）
2. Routineプロンプトを「`deal-next-action-reminder` スキルを実行して」＋高リスク箇所の
   ガードレール再掲、という形に組み直し（checkout手順を削除）
3. 同日 10:21 JST に取り直しの実行を発火（本日分7件が未投稿のため）

**学び**：Routineは失敗しても静かに終わる。投稿0件が「対象なし」なのか「途中で失敗」なのか
外から区別できないため、**対象0件のときも完了報告に明示的にそう書かせる**設計が効く
（スキルSTEP 2・STEP 8はそうなっている）。定期実行の翌朝は投稿ログに当日行があるかを
確認するのが最も確実な生存確認。

### 2026-07-30 10:21 JST 取り直し実行（スキル前提の新プロンプト）— ❌ これも不発

新プロンプト（checkout手順を削除し `deal-next-action-reminder` スキルを呼ぶ形）で
`fire_trigger` したが、**12分後の確認時点で投稿ログ・チャンネルキャッシュ・商談チャンネル・
自分宛DM のすべてに痕跡がなかった**。定期実行に続き2連続で不発。

つまり原因はブランチcheckout方式ではなく、**Routineの発火セッション側にある**。

### 原因の第一仮説：発火セッションの許可ツールにMCPが含まれていない

`create_trigger` / `update_trigger` のレスポンスに含まれる `session_context.allowed_tools` は：

```
["preset:default", "Task", "Bash", "Glob", "Grep", "Read", "Edit", "MultiEdit", "Write",
 "NotebookEdit", "WebFetch", "TodoWrite", "WebSearch", "BashOutput", "KillBash", "Skill",
 "Tmux", "Monitor", "SendUserFile", "REPL"]
```

**`mcp__*` 系が1つも入っていない。** `mcp_connections` にFFDE / Slackが紐づいていても、
ツール呼び出しが許可されていなければ、発火セッションはClickHouseに問い合わせもSlack投稿も
できない。STEP 1で詰まって静かに終わる挙動と符合する。

ただしこの仮説は、2026-07-29の疎通確認実行が「いけてる」と報告された事実と整合しない
（あの実行でFFDE/Slackが使えていたなら、許可はあったことになる）。**確定させるには
発火セッション（`cse_01TwhDe652zQFiwC93UgGgkz`）の中身を見る必要がある。**

`update_trigger` に `allowed_tools` を変更するパラメータは無いため、ツール経由では直せない。
**恒久対応の第一候補は、claude.ai の Routines UI からRoutineを作り直すこと**
（UI作成なら権限とコネクタが正しく設定される）。cronとプロンプトは本ファイルの内容を流用する。

### 2026-07-30 手動配信（暫定対応）

Routineが直るまでの暫定として、対話セッションから7件を手動配信した。

| 商談 | 状態 | チャンネル |
|---|---|---|
| アドバンテッジリスクマネジメント | 🔴 9日超過 ⚠️要棚卸し | `C0BG873MPR9` |
| エイチ・アイ・エス | 🔴 6日超過 | `C0BFVJ3H62W` |
| エスリード_評価査定表_Shot | 🔴 6日超過 | `C0BJ9DKVBNC` |
| 富士フイルムメディカル | 🔴 3日超過 | `C0AN9G48KK5` |
| インヴァランス_AIインタフェースfor不動産 | 🟡 本日 | `C0AN06U7U9M`（新規解決） |
| エスリード_MCP | 🟡 本日 | `C0BHJQNP7GC`（新規解決） |
| 日本3Dプリンター_MCP | 🟡 本日 | `C0BJME28Y4W` |

新規2件のチャンネルも名前検索1回で解決でき、キャッシュに追記済み。
**設計（クエリ・仕分け・チャンネル解決・メッセージ書式・状態管理）は2日連続で正しく機能している。
壊れているのはRoutineの実行環境だけ**という切り分けができた。
